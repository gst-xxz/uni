import { createStore } from "vuex";

const store = createStore({
  state: {},
  getters: {
    // user: (state) => state.user,
  },
  actions: {
    // async fetchBalance({ commit, state }) {},
  },
  mutations: {
    // initBalance(state, payload) {
    //   state.balance = payload.balance;
    // },
  },
});

export default store;
